---
apiVersion: v1
kind: Template
metadata:
  name: route-template
objects:
  - apiVersion: v1
    kind: Route
    metadata:
      name: flareclient-api
    spec:
      host: flareclient-api.${NAMESPACE}.${APPS_SUBDOMAIN}
      to:
        kind: Service
        name: flareclient-api
      tls:
        termination: passthrough
parameters:
  - name: NAMESPACE
    displayName: Namespace (Openshift project)
    description: Openshift project this application resides in
    required: true
  - name: APPS_SUBDOMAIN
    displayName: Application Subdomain
    description: The subdomain that applications are placed under
    required: true

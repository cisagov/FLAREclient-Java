version: '2'
services:
    flareclient-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb flareclient-mongodb-config
    flareclient-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    flareclient-mongodb-config:
        image: mongo:3.6.3
        container_name: flareclient-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
